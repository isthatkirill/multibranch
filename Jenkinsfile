pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'ls -la'
                sleep 3000
                echo "This is MAIN branch"
            }
        }
    }
}
