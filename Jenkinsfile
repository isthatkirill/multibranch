pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'ls -la'
                sleep 1000000
                echo "This is MAIN branch"
            }
        }
    }
}
