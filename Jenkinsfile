pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'ls -la'
                sleep 200
                echo "Hello! I am from main!"
            }
        }
    }
}
