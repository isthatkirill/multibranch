pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'ls -la'
                sleep 300
                echo "This is MAIN branch"
            }
        }
    }
}
