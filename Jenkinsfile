pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'ls -la'
                sleep 200
                echo "This is MAIN branch"
            }
        }
    }
}
