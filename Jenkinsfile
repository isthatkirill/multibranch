pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'ls -la'
                sleep 240
                echo "Hello! I am from main!"
            }
        }
    }
}
