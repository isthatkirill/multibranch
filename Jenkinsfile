pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'ls -la'
                sleep 1
                echo "This is MAIN branch"
            }
        }
    }
}
