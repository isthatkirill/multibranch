pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'ls -la'
                sleep 100
                echo "This is MAIN branch"
            }
        }
    }
}
