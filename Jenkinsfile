pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'ls -la'
                sleep 120
                echo "This is MAIN branch"
            }
        }
    }
}
